docker build -t api .    --> tp build

docker run -it --rm -p 8080:8080 api     --> to run 

docker compose up -d            --> to compose and run 

docker compose down             --> to down compose

npx gitignore Node              --> to create new gitignore automatically

command to generate SSH code    --> ssh-keygen -t rsa -b 4096 -C "<identity>"



